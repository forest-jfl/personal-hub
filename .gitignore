# 依赖与构建产物
node_modules/
dist/
*.tsbuildinfo

# 运行时数据
uploads/
*.log
.env
# 系统/编辑器
.DS_Store
.idea/
.vscode/
.pages-worktree/

# 发布备份：scripts/deploy-frontend.sh 在服务端生成（本机没有，两边共用这份 .gitignore）
.deploy-backups/

# 本地实测产物
.verify-shots/
.cookies.txt
.probe.xml

# 验收截图：每跑一次 verify-ui.mjs 内容就变，属本地产物，不入库
# （README 里说的「每页截图留档」= 留在这台机器上，不是留进版本库）
docs/screenshots/

# Windows 系统缓存误生成目录
%SystemDrive%/
